#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 *_strlen - Entry point
 *@s : caractere a verifier
 *int *n: caractere a verifier
 *fonction: ecris alphabet
 * Return: Always 0 (Success)
 */

int _strlen(char *s)
{
return (strlen(s));
}
