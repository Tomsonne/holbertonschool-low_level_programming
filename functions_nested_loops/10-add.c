#include <stdio.h>
#include"main.h"
/**
 *add - Entry point
 *@x : caractere a verifier
 *@y : azeazea
 *int c: caractere a verifier
 *fonction: ecris alphabet
 * Return: Always 0 (Success)
 */

int add(int x, int y)
{
int c;

c = x + y;
return (c);
}
