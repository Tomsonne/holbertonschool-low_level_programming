#include <stdio.h>
#include"main.h"
/**
 *mul - Entry point
 *@b : caractere a verifier
 *@a : azeazea
 *int c: caractere a verifier
 *fonction: ecris alphabet
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
return (a * b);
}
